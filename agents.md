# Agent Instructions

- Always increment the library version when making changes and add a corresponding entry to the `PackageReleaseNotes` field inside `baseVISION.Tool.Connectors.Zoho.csproj` describing what changed.
- Update `README.md` in parallel to summarize the same changes so that release information is visible without needing to unpack the NuGet package.
- Keep both descriptions aligned so that users can quickly trace the reasoning behind each new release.
